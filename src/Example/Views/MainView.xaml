<Page x:Class="Example.Views.MainView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:dock="using:WinUI.Dock"
      xmlns:vm="using:Example.ViewModels"
      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Page.DataContext>
        <vm:MainViewModel />
    </Page.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <CommandBar Grid.Row="0"
                    HorizontalAlignment="Left">
            <CommandBar.PrimaryCommands>
                <AppBarButton Icon="AttachCamera"
                              Label="Attach Camera" />
                <AppBarSeparator />
                <AppBarButton Icon="Like"
                              Label="Like" />
                <AppBarButton Icon="Dislike"
                              Label="Dislike" />
                <AppBarSeparator />
                <AppBarButton Icon="Orientation"
                              Label="Orientation" />
            </CommandBar.PrimaryCommands>
        </CommandBar>

        <dock:DockManager Grid.Row="1"
                          Adapter="{Binding}"
                          Behavior="{Binding}">
            <dock:LayoutPanel Orientation="Vertical">
                <dock:LayoutPanel DockHeight="5"
                                  Orientation="Horizontal">
                    <dock:DocumentGroup DockWidth="4"
                                        ShowWhenEmpty="True">
                        <dock:Document Title="MainView.xaml"
                                       CanClose="True" />
                        <dock:Document Title="MainViewModel.cs"
                                       CanClose="True" />
                    </dock:DocumentGroup>

                    <dock:DocumentGroup DockWidth="1"
                                        TabPosition="Bottom">
                        <dock:Document Title="Solution Explorer"
                                       CanClose="True"
                                       CanPin="True" />
                        <dock:Document Title="Git Changes"
                                       CanClose="True"
                                       CanPin="True" />
                    </dock:DocumentGroup>
                </dock:LayoutPanel>

                <dock:LayoutPanel DockWidth="1"
                                  Orientation="Horizontal">
                    <dock:DocumentGroup DockWidth="1"
                                        TabPosition="Bottom">
                        <dock:Document Title="Error List"
                                       CanClose="True"
                                       CanPin="True" />
                    </dock:DocumentGroup>

                    <dock:DocumentGroup DockWidth="1"
                                        TabPosition="Bottom"
                                        UseCompactTabs="True">
                        <dock:Document Title="Output"
                                       CanClose="True"
                                       CanPin="True" />
                        <dock:Document Title="Terminal"
                                       CanClose="True"
                                       CanPin="True" />
                    </dock:DocumentGroup>
                </dock:LayoutPanel>
            </dock:LayoutPanel>
        </dock:DockManager>
    </Grid>
</Page>
