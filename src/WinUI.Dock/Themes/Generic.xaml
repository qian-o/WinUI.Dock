<!--
    This code was generated by a tool.
    Changes to this file may cause incorrect behavior and will be lost if the code is regenerated.
-->
<!--
Source files:
DockManager.xaml
Document.xaml
DocumentGroup.xaml
LayoutPanel.xaml
-->
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:communityToolkit="using:CommunityToolkit.WinUI.Controls" xmlns:controls="using:WinUI.Dock.Controls" xmlns:dock="using:WinUI.Dock">
  <Style TargetType="dock:DockManager">
    <Setter Property="AllowDrop" Value="True" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="dock:DockManager">
          <Grid Background="Transparent">
            <Grid.RowDefinitions>
              <RowDefinition Height="Auto" />
              <RowDefinition Height="*" />
              <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="Auto" />
              <ColumnDefinition Width="*" />
              <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <VisualStateManager.VisualStateGroups>
              <VisualStateGroup x:Name="OperationDockTargets">
                <VisualState x:Name="ShowSideDockTargets">
                  <VisualState.Setters>
                    <Setter Target="DockTargets.Visibility" Value="Visible" />
                    <Setter Target="DockTargetCenter.Visibility" Value="Collapsed" />
                  </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="ShowAllDockTargets">
                  <VisualState.Setters>
                    <Setter Target="DockTargets.Visibility" Value="Visible" />
                    <Setter Target="DockTargetCenter.Visibility" Value="Visible" />
                  </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="HideDockTargets">
                  <VisualState.Setters>
                    <Setter Target="DockTargets.Visibility" Value="Collapsed" />
                  </VisualState.Setters>
                </VisualState>
              </VisualStateGroup>
            </VisualStateManager.VisualStateGroups>
            <!--
                            If you use GridSplitter directly in the cs file,
                            the style of GridSplitter will have issues.
                            So, create a hidden GridSplitter in the xaml file first,
                            to ensure the style of GridSplitter is displayed correctly.
                        -->
            <communityToolkit:GridSplitter Visibility="Collapsed" />
            <controls:Sidebar Grid.Row="1" Grid.Column="0" DockManager="{Binding RelativeSource={RelativeSource TemplatedParent}}" DockSide="Left" Documents="{Binding LeftSide, RelativeSource={RelativeSource TemplatedParent}}" />
            <controls:Sidebar Grid.Row="0" Grid.Column="1" DockManager="{Binding RelativeSource={RelativeSource TemplatedParent}}" DockSide="Top" Documents="{Binding TopSide, RelativeSource={RelativeSource TemplatedParent}}" />
            <controls:Sidebar Grid.Row="1" Grid.Column="2" DockManager="{Binding RelativeSource={RelativeSource TemplatedParent}}" DockSide="Right" Documents="{Binding RightSide, RelativeSource={RelativeSource TemplatedParent}}" />
            <controls:Sidebar Grid.Row="2" Grid.Column="1" DockManager="{Binding RelativeSource={RelativeSource TemplatedParent}}" DockSide="Bottom" Documents="{Binding BottomSide, RelativeSource={RelativeSource TemplatedParent}}" />
            <ContentPresenter Grid.Row="1" Grid.Column="1" Content="{TemplateBinding Panel}" />
            <Border x:Name="PART_PopupContainer" Grid.Row="1" Grid.Column="1" />
            <Grid x:Name="DockTargets" Grid.Row="1" Grid.Column="1" Visibility="Collapsed">
              <controls:DockTargetButton x:Name="DockTargetCenter" HorizontalAlignment="Center" VerticalAlignment="Center" DockTarget="Center" Target="{Binding RelativeSource={RelativeSource TemplatedParent}}" />
              <controls:DockTargetButton HorizontalAlignment="Left" VerticalAlignment="Center" DockTarget="DockLeft" Target="{Binding RelativeSource={RelativeSource TemplatedParent}}" />
              <controls:DockTargetButton HorizontalAlignment="Center" VerticalAlignment="Top" DockTarget="DockTop" Target="{Binding RelativeSource={RelativeSource TemplatedParent}}" />
              <controls:DockTargetButton HorizontalAlignment="Right" VerticalAlignment="Center" DockTarget="DockRight" Target="{Binding RelativeSource={RelativeSource TemplatedParent}}" />
              <controls:DockTargetButton HorizontalAlignment="Center" VerticalAlignment="Bottom" DockTarget="DockBottom" Target="{Binding RelativeSource={RelativeSource TemplatedParent}}" />
            </Grid>
            <controls:AnimationPreview x:Name="PART_Preview" Grid.Row="1" Grid.Column="1" Visibility="Collapsed" />
          </Grid>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style TargetType="dock:Document">
    <Setter Property="Padding" Value="16" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="dock:Document">
          <Border Padding="{TemplateBinding Padding}">
            <ContentPresenter Content="{TemplateBinding Content}" />
          </Border>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style TargetType="dock:LayoutPanel">
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="dock:LayoutPanel">
          <Grid x:Name="PART_Root" ColumnSpacing="12" RowSpacing="12" />
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style TargetType="dock:DocumentGroup">
    <Setter Property="AllowDrop" Value="True" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="dock:DocumentGroup">
          <Grid>
            <VisualStateManager.VisualStateGroups>
              <VisualStateGroup x:Name="IsActive">
                <VisualState x:Name="Active">
                  <VisualState.Setters>
                    <Setter Target="PART_Root.BorderBrush" Value="{ThemeResource DockBrush}" />
                  </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="Inactive">
                  <VisualState.Setters>
                    <Setter Target="PART_Root.BorderBrush" Value="{ThemeResource TabViewBorderBrush}" />
                  </VisualState.Setters>
                </VisualState>
              </VisualStateGroup>
              <VisualStateGroup x:Name="TabPosition">
                <VisualState x:Name="Top">
                  <VisualState.Setters>
                    <Setter Target="Scale.ScaleY" Value="1" />
                  </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="Bottom">
                  <VisualState.Setters>
                    <Setter Target="Scale.ScaleY" Value="-1" />
                  </VisualState.Setters>
                </VisualState>
              </VisualStateGroup>
              <VisualStateGroup x:Name="IsTabWidthBasedOnContent">
                <VisualState x:Name="TabWidthEqual">
                  <VisualState.Setters>
                    <Setter Target="PART_Root.TabWidthMode" Value="Equal" />
                  </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="TabWidthSizeToContent">
                  <VisualState.Setters>
                    <Setter Target="PART_Root.TabWidthMode" Value="SizeToContent" />
                  </VisualState.Setters>
                </VisualState>
              </VisualStateGroup>
              <VisualStateGroup x:Name="OperationDockTargets">
                <VisualState x:Name="ShowDockTargets">
                  <VisualState.Setters>
                    <Setter Target="DockTargets.Visibility" Value="Visible" />
                  </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="HideDockTargets">
                  <VisualState.Setters>
                    <Setter Target="DockTargets.Visibility" Value="Collapsed" />
                  </VisualState.Setters>
                </VisualState>
              </VisualStateGroup>
            </VisualStateManager.VisualStateGroups>
            <TabView x:Name="PART_Root" RenderTransformOrigin="0.5,0.5" SelectedIndex="{Binding SelectedIndex, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}" Style="{StaticResource ActiveTabViewStyle}">
              <TabView.RenderTransform>
                <ScaleTransform x:Name="Scale" />
              </TabView.RenderTransform>
            </TabView>
            <Grid x:Name="DockTargets" Width="124" Height="124" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Collapsed">
              <Path Data="m44,0 l36,0 a4,4,0,0,1,4,4 l0,32 a4,4,0,0,0,4,4 l32,0 a4,4,0,0,1,4,4 l0,36 a4,4,0,0,1,-4,4 l-32,0 a4,4,0,0,0,-4,4 l0,32 a4,4,0,0,1,-4,4 l-36,0 a4,4,0,0,1,-4,-4 l0,-32 a4,4,0,0,0,-4,-4 l-32,0 a4,4,0,0,1,-4,-4 l0,-36 a4,4,0,0,1,4,-4 l32,0 a4,4,0,0,0,4,-4 l0,-32 a4,4,0,0,1,4,-4 z" Fill="{ThemeResource CardBackgroundFillColorDefaultBrush}" Stretch="Uniform" Stroke="{ThemeResource CardStrokeColorDefaultBrush}" StrokeThickness="1" />
              <Grid Width="120" Height="120" Margin="2">
                <Grid.RowDefinitions>
                  <RowDefinition Height="*" />
                  <RowDefinition Height="*" />
                  <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="*" />
                  <ColumnDefinition Width="*" />
                  <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <controls:DockTargetButton Grid.Row="1" Grid.Column="1" Margin="2" DockTarget="Center" Target="{Binding RelativeSource={RelativeSource TemplatedParent}}" />
                <controls:DockTargetButton Grid.Row="1" Grid.Column="0" Margin="2" DockTarget="SplitLeft" Target="{Binding RelativeSource={RelativeSource TemplatedParent}}" />
                <controls:DockTargetButton Grid.Row="0" Grid.Column="1" Margin="2" DockTarget="SplitTop" Target="{Binding RelativeSource={RelativeSource TemplatedParent}}" />
                <controls:DockTargetButton Grid.Row="1" Grid.Column="2" Margin="2" DockTarget="SplitRight" Target="{Binding RelativeSource={RelativeSource TemplatedParent}}" />
                <controls:DockTargetButton Grid.Row="2" Grid.Column="1" Margin="2" DockTarget="SplitBottom" Target="{Binding RelativeSource={RelativeSource TemplatedParent}}" />
              </Grid>
            </Grid>
            <controls:AnimationPreview x:Name="PART_Preview" Visibility="Collapsed" />
          </Grid>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
</ResourceDictionary>